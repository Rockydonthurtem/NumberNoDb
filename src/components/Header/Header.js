import React from "react";
import "./Header.css"

function Header() {
    return (
        <div>
            <header className="App-Header">
                <h2>Things you never knew</h2>
                <h5>..what do all odd numbers have in common?(the letter e)</h5>
            </header>
        </div>
    );
}

export default Header;